<h1>Users bitches:</h1>

<table class="table">
  <%= for user <- @users do %>
  <tr>
    <td><b><%= user.name %> <%= user.surname %></b> (<%= user.id %>) <%= user.mail %> </td>
    <td><%= link "view", to: user_path(@conn, :show, user.id)%></td>
  </tr>
<% end %>
</table>
